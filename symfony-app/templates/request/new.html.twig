{% extends 'base.html.twig' %}

{% block title %}Nouvelle Requête - MAESTRO{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-8">
        <div class="card border-0 shadow-sm">
            <div class="card-header" style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);">
                <h4 class="mb-0 text-white">
                    ➕ Nouvelle Requête d'Analyse
                </h4>
                <small class="text-white-50">Projet: {{ project_name }}</small>
            </div>
            <div class="card-body">
                <p class="text-muted mb-4">
                    Décrivez simplement votre besoin ci-dessous. L'agent Analyzer MAESTRO analysera automatiquement
                    votre demande pour déterminer le type, la priorité, la complexité et les agents appropriés à mobiliser.
                </p>

                <form method="post" data-turbo="false">
                    <input type="hidden" name="_token" value="{{ csrf_token('request_form') }}">

                    <div class="mb-4">
                        <label for="description" class="form-label fw-bold">Décrivez votre besoin *</label>
                        <textarea class="form-control form-control-lg" id="description" name="description" rows="12"
                                  placeholder="Décrivez librement votre besoin...&#10;&#10;Exemples :&#10;- Je veux créer une API REST pour gérer les utilisateurs avec authentification JWT&#10;- Il y a un bug dans le module de paiement qui ne valide pas les cartes Visa&#10;- J'ai besoin d'améliorer les performances de la page d'accueil&#10;- Nous devons refactoriser le code legacy du module de facturation&#10;&#10;L'IA analysera automatiquement :&#10;✓ Le type de demande (feature, bug, amélioration...)&#10;✓ La priorité (basse, moyenne, haute, critique)&#10;✓ La complexité (XS, S, M, L, XL)&#10;✓ Les agents nécessaires (PM, Cadrage, US, Dev, Test, Deploy)">{{ description|default('') }}</textarea>
                        <small class="form-text text-muted">
                            💡 <strong>Astuce :</strong> Plus votre description est détaillée (fonctionnalités, contraintes, contexte),
                            plus l'analyse de l'agent Analyzer sera précise et pertinente.
                        </small>
                    </div>

                    <div class="alert alert-info border-0">
                        <div class="d-flex align-items-start">
                            <div class="me-3 fs-4">🤖</div>
                            <div>
                                <strong>Intelligence artificielle au travail</strong><br>
                                <small>
                                    1. L'<strong>agent Analyzer</strong> analyse votre demande brute<br>
                                    2. Il détermine automatiquement type, priorité et complexité<br>
                                    3. Il identifie les agents appropriés à mobiliser (PM → Cadrage → US → Dev...)<br>
                                    4. Vous recevez une analyse complète avec estimation précise
                                </small>
                            </div>
                        </div>
                    </div>

                    <div class="d-flex justify-content-between align-items-center">
                        <a href="{{ path('app_project_dashboard', {slug: app.session.get('current_project_slug')}) }}" class="btn btn-outline-secondary">
                            ← Annuler
                        </a>
                        <button type="submit" class="btn btn-primary btn-lg" id="submitBtn">
                            🚀 Lancer l'Analyse
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>

<script>
document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('form');
    const submitBtn = document.getElementById('submitBtn');

    form.addEventListener('submit', function(e) {
        console.log('Form submit event triggered');
        console.log('Description value:', document.getElementById('description').value);
        submitBtn.disabled = true;
        submitBtn.innerHTML = '⏳ Envoi en cours...';
    });
});
</script>
{% endblock %}

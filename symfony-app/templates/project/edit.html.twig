{% extends 'base.html.twig' %}

{% block title %}Éditer {{ project.name }} - MAESTRO{% endblock %}

{% block body %}
<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card border-0 shadow-sm">
            <div class="card-header bg-white">
                <h5 class="mb-0 text-dark">Éditer le Projet</h5>
            </div>
            <div class="card-body">
                <form method="post">
                    <div class="mb-3">
                        <label for="name" class="form-label">Nom du projet *</label>
                        <input type="text" class="form-control" id="name" name="name"
                               value="{{ project.name }}" required>
                    </div>

                    <div class="mb-3">
                        <label class="form-label">Slug</label>
                        <input type="text" class="form-control" value="{{ project.slug }}" disabled>
                        <small class="form-text text-muted">Le slug ne peut pas être modifié</small>
                    </div>

                    <div class="mb-3">
                        <label for="description" class="form-label">Description</label>
                        <textarea class="form-control" id="description" name="description" rows="4">{{ project.description|default('') }}</textarea>
                    </div>

                    <div class="mb-3">
                        <label for="technical_stack" class="form-label">Stack Technique</label>
                        <textarea class="form-control" id="technical_stack" name="technical_stack" rows="6" placeholder="Ex: PHP 8.2, Symfony 7.3, PostgreSQL 15, React 18, etc.">{{ project.project_cadrage_decoded.architecture.stack_technique|default('') }}</textarea>
                        <small class="form-text text-muted">Décrivez les technologies, frameworks, versions, etc. utilisés dans ce projet</small>
                    </div>

                    <div class="d-flex justify-content-between">
                        <a href="{{ path('app_projects_list') }}" class="btn btn-outline-secondary">Annuler</a>
                        <button type="submit" class="btn btn-primary">Enregistrer</button>
                    </div>
                </form>

                <hr class="my-4">
                <h6 class="text-muted mb-3">🔗 Dépôt Gitea</h6>

                {% if project.gitea_repo_id %}
                    <div class="card border-0 shadow-sm bg-light">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center mb-3">
                                <div>
                                    <h6 class="mb-1 text-success">✓ Dépôt Gitea Configuré</h6>
                                    <small class="text-muted">{{ project.gitea_repo_name }}</small>
                                </div>
                                <a href="{{ project.gitea_repo_url }}" target="_blank" class="btn btn-sm btn-outline-primary">
                                    Ouvrir dans Gitea →
                                </a>
                            </div>
                            <div class="row g-2">
                                <div class="col-12">
                                    <small class="text-muted d-block">URL du dépôt:</small>
                                    <code class="d-block">{{ project.gitea_repo_url }}</code>
                                </div>
                                <div class="col-12">
                                    <small class="text-muted d-block">Clone HTTPS:</small>
                                    <code class="d-block">git clone {{ project.gitea_repo_url }}.git</code>
                                </div>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="card border-0 shadow-sm bg-light">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h6 class="mb-1 text-warning">⚠️ Dépôt Gitea Non Initialisé</h6>
                                    <small class="text-muted">Créez un dépôt Gitea pour ce projet afin d'activer le workflow CI/CD</small>
                                </div>
                                <form method="post" action="{{ path('app_gitea_init', {slug: project.slug}) }}" class="d-inline" data-turbo="false">
                                    <input type="hidden" name="_token" value="{{ csrf_token('gitea_init_' ~ project.slug) }}">
                                    <button type="submit" class="btn btn-primary">
                                        🚀 Initialiser le Dépôt Gitea
                                    </button>
                                </form>
                            </div>
                            <div class="alert alert-info mt-3 mb-0">
                                <strong>Ce qui sera créé:</strong>
                                <ul class="mb-0 mt-2">
                                    <li>Un dépôt privé dans l'organisation <code>maestro</code></li>
                                    <li>Un fichier README.md initial avec les infos du projet</li>
                                    <li>Un webhook vers n8n pour la CI/CD</li>
                                    <li>Configuration .gitignore Symfony</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                {% endif %}

                <hr class="my-4">
                <h6 class="text-muted mb-3">🚀 Environnements Coolify</h6>

                {% if project.config_decoded.coolify_project_uuid is defined and project.config_decoded.coolify_project_uuid %}
                    <div class="card border-0 shadow-sm bg-light">
                        <div class="card-body">
                            <div class="mb-3">
                                <h6 class="mb-1 text-success">✓ Coolify Configuré</h6>
                                <small class="text-muted">Environnements de déploiement prêts</small>
                            </div>
                            <div class="row g-2">
                                <div class="col-md-6">
                                    <div class="card border-warning">
                                        <div class="card-body">
                                            <h6 class="text-warning mb-2">🟡 STAGING</h6>
                                            <small class="text-muted d-block mb-2">Environnement de test</small>
                                            {% if project.config_decoded.coolify_staging_url is defined %}
                                            <a href="{{ project.config_decoded.coolify_staging_url }}" target="_blank" class="btn btn-sm btn-outline-warning w-100">
                                                Ouvrir →
                                            </a>
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="card border-danger">
                                        <div class="card-body">
                                            <h6 class="text-danger mb-2">🔴 PRODUCTION</h6>
                                            <small class="text-muted d-block mb-2">Environnement de production</small>
                                            {% if project.config_decoded.coolify_production_url is defined %}
                                            <a href="{{ project.config_decoded.coolify_production_url }}" target="_blank" class="btn btn-sm btn-outline-danger w-100">
                                                Ouvrir →
                                            </a>
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="mt-3">
                                <a href="{{ path('app_project_deployments', {slug: project.slug}) }}" class="btn btn-sm btn-primary w-100">
                                    <i class="bi bi-rocket"></i> Gérer les Déploiements
                                </a>
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="card border-0 shadow-sm bg-light">
                        <div class="card-body">
                            <div class="d-flex justify-content-between align-items-center">
                                <div>
                                    <h6 class="mb-1 text-warning">⚠️ Coolify Non Configuré</h6>
                                    <small class="text-muted">Créez automatiquement les environnements de déploiement</small>
                                </div>
                                {% if project.gitea_repo_id %}
                                <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#coolifyInitModal">
                                    🚀 Initialiser Coolify
                                </button>
                                {% else %}
                                <button type="button" class="btn btn-secondary" disabled title="Initialisez d'abord le dépôt Gitea">
                                    🚀 Initialiser Coolify
                                </button>
                                {% endif %}
                            </div>
                            <div class="alert alert-info mt-3 mb-0">
                                <strong>Ce qui sera créé automatiquement:</strong>
                                <ul class="mb-0 mt-2">
                                    <li>Un projet Coolify pour <strong>{{ project.name }}</strong></li>
                                    <li>Application <strong>STAGING</strong>: <code>staging-{{ project.slug }}.maestro.ara-solutions.cloud</code></li>
                                    <li>Application <strong>PRODUCTION</strong>: <code>{{ project.slug }}.maestro.ara-solutions.cloud</code></li>
                                    <li>Configuration SSL automatique (Let's Encrypt)</li>
                                    <li>Build automatique via Nixpacks (PHP détecté)</li>
                                    <li>Health checks configurés</li>
                                </ul>
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>
</div>

{# Modal Confirmation Coolify Init #}
<div class="modal fade" id="coolifyInitModal" tabindex="-1" aria-labelledby="coolifyInitModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header bg-primary text-white">
                <h5 class="modal-title" id="coolifyInitModalLabel">
                    <i class="bi bi-rocket"></i> Initialiser Coolify
                </h5>
                <button type="button" class="btn-close btn-close-white" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <div class="alert alert-info">
                    <i class="bi bi-info-circle"></i>
                    <strong>Attention :</strong> Cette opération va créer automatiquement les environnements de déploiement dans Coolify.
                </div>

                <h6 class="mb-3">Ce qui sera créé :</h6>
                <ul class="list-unstyled">
                    <li class="mb-2">
                        <i class="bi bi-check-circle text-success"></i>
                        <strong>Projet Coolify :</strong> {{ project.name }}
                    </li>
                    <li class="mb-2">
                        <i class="bi bi-check-circle text-warning"></i>
                        <strong>Environnement STAGING :</strong><br>
                        <code class="ms-4">staging-{{ project.slug }}.maestro.ara-solutions.cloud</code>
                    </li>
                    <li class="mb-2">
                        <i class="bi bi-check-circle text-danger"></i>
                        <strong>Environnement PRODUCTION :</strong><br>
                        <code class="ms-4">{{ project.slug }}.maestro.ara-solutions.cloud</code>
                    </li>
                    <li class="mb-2">
                        <i class="bi bi-check-circle text-primary"></i>
                        <strong>Configuration SSL</strong> (Let's Encrypt automatique)
                    </li>
                    <li class="mb-2">
                        <i class="bi bi-check-circle text-primary"></i>
                        <strong>Build automatique</strong> (Nixpacks PHP)
                    </li>
                </ul>

                <div class="alert alert-warning mb-0">
                    <i class="bi bi-clock-history"></i>
                    <strong>Durée estimée :</strong> 30-60 secondes
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="bi bi-x-circle"></i> Annuler
                </button>
                <form method="post" action="{{ path('app_coolify_init', {slug: project.slug}) }}" class="d-inline" data-turbo="false">
                    <input type="hidden" name="_token" value="{{ csrf_token('coolify_init_' ~ project.slug) }}">
                    <button type="submit" class="btn btn-primary">
                        <i class="bi bi-rocket"></i> Confirmer l'initialisation
                    </button>
                </form>
            </div>
        </div>
    </div>
</div>
{% endblock %}
